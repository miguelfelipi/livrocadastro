<resources>
    <!-- Tema fixo dark usando Material Components -->
    <style name="Theme.Livrocadastro" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/teal_200</item>
        <item name="android:windowBackground">@color/darkBackground</item>
        <!-- Define o estilo padrão para botões -->
        <item name="buttonStyle">@style/CustomButtonStyle</item>
        <!-- Define a cor padrão dos textos -->
        <item name="android:textColorPrimary">@color/lightGray</item>
    </style>

    <!-- Estilo customizado para botões usando backgroundTint -->
    <style name="CustomButtonStyle" parent="Widget.MaterialComponents.Button">
        <item name="android:backgroundTint">@color/teal_900</item>
        <item name="android:textColor">@color/lightGray</item>
        <item name="android:textSize">18sp</item>
        <item name="android:minHeight">56dp</item>
    </style>

    <!-- (Opcional) Estilo customizado para EditText -->
    <style name="CustomEditTextStyle" parent="Widget.MaterialComponents.TextInputEditText.OutlinedBox">
        <item name="android:textColor">@color/lightGray</item>
        <item name="android:textColorHint">@color/lightGray</item>
        <item name="android:minHeight">40dp</item>
    </style>
</resources>
